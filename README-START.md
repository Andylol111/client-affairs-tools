# Local UI only

The live site is AWS. See [`README.md`](README.md).

To run the UI on a laptop (does not deploy, does not share the live database):

```bash
./start-all.sh
```

Busy ports: `./kill-ports.sh`.
